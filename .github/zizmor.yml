rules:
  stale-action-refs:
    ignore:
      # Home Assistant publishes this helper from an untagged commit.
      # Keep the full SHA pin; repository settings allowlist the action source.
      - build-app.yaml:40
